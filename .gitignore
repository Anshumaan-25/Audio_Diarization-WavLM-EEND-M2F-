# WavLM+EEND-M2F pipeline — never commit weights, audio, or session artifacts.
models/
session_out/
*.manifest.jsonl
*.wav
work/
.venv*/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
*_tmp.html

# The SOTA A/B sandbox lives in sota_sandbox/ and is tracked. Its OWN
# sota_sandbox/.gitignore keeps the local HF token (.hf_token) and its outputs
# out of git — never commit those.
